# NODE.yml
#   generated by branectl v1.0.0
# 
# This file defines the environment of the local node.
# Edit this file to change service properties. Some require a restart
# of the service (typically any 'ports' or 'topics' related setting), but most
# will be reloaded dynamically by the services themselves.
# 
# For an overview of what you can do in this file, refer to
# https://wiki.enablingpersonalizedinterventions.nl/user-guide/system-admins/docs/config/node.md
# 


---
proxy: ~
paths:
  certs: "./config_test/certs"
  packages: "/tmp/packages"
ports:
  prx: "0.0.0.0:50050"
services:
  prx: "brane-prx:50051"
node:
  kind: central
  paths:
    infra: "./config/infra.yml"
    secrets: "./config/secrets.yml"
  ports:
    api: "0.0.0.0:50051"
    drv: "0.0.0.0:50053"
  services:
    brokers:
      - "aux-kafka:9092"
    scylla: "aux-scylla:9042"
    api: "http://brane-api:50051"
  topics:
    planner_command: plr-cmd
    planner_results: plr-res
